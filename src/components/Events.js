import React from 'react'

export const Events = () => {
  return (
    <div>
        <div>
            <button onClick={() => console.log('Paulo')}>Clique aqui</button>
        </div>
    </div>
  )
}
